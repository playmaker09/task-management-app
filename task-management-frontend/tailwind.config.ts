import type { Config } from "tailwindcss";

export default {
    content: [],
    theme: {
        extend: {
            fontFamily: {
                sans: ["Roboto", "ui-sans-serif", "system-ui"],
            },
        },
    },
    plugins: [],
} satisfies Config;
